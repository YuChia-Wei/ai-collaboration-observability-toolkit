# Claude Code entry point

Follow `AGENTS.md` as the canonical repository contract. The implementation brief is `docs/IMPLEMENTATION-BRIEF.md`.

Do not broaden telemetry collection to capture content. Do not silently replace the Collector routing or redaction strategy. Use the existing Python operations layer and keep Bash/PowerShell wrappers thin. Validation evidence must distinguish static checks, container runtime checks, and manual UI inspection.
